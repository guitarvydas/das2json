# layer 3
## parameters
  Parent
  Child
## imports
    shapes
    onSameDiagram
    inside
    names
    ports
	contains
## query
  contains(Parent,Child)
## display
  das_fact(contains,${Parent},${Child}).
